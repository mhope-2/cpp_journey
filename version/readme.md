#### check c++ compiler version (g++)
```shell
    g++ version.cpp -o version
    ./version
```

#### use specific c++ compiler version (g++)
```shell
    g++ version.cpp -o version -std=c++17
    ./version
```